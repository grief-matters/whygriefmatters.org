*[_type == 'contentGroup']{
  "id": coalesce(slug.current, _id),
  name,
  title,
  description,
  "slug": slug.current,
  "imageAssetId": imageRef->._id,
  "contentBlocks": coalesce(contentBlocks[]->._id, [])
}
