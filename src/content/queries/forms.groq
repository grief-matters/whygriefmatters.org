*[_type == "form"]{
  "id": _id,
  "formId": formId.current,
  title,
  description,
  fields[]{
    label,
    description,
    name,
    type,
    inputOptions,
    placeholder,
    "required": coalesce(required, true)
  },
  submitButtonText,
  successMessage
}
