*[_type == 'website']{
  "id": _id,
  "updatedAt": _updatedAt,
  resourceUrl,
  title,
  description,
  logo,
  "categories": coalesce(categories[]->._id, []),
  "populations": coalesce(populations[]->._id, []),
}